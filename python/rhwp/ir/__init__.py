"""rhwp.ir — Document IR v1 공개 데이터 모델.

사용: ``from rhwp.ir.nodes import HwpDocument, ParagraphBlock, ...``

프로젝트 규칙에 따라 본 파일은 docstring 만 포함한다 (재-export 없음 — 순환 import 방지).
타입 체커용 재-export 는 ``__init__.pyi`` 에서 선언된다.
"""
